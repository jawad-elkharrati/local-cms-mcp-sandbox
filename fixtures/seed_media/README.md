# Seed media

The deterministic seed command generates four tiny fictional PNG objects and uploads them to local
MinIO. Binary generated fixtures are intentionally not copied from any external source.
